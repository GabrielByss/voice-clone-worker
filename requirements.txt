# Instalowane na obrazie pytorch/pytorch:2.8.0 (torch 2.8 + torchaudio już są). Pakiety modeli instalujemy z --no-deps
# (Dockerfile), żeby nie ciągnąć gradio/modelscope/funasr/datasets i nie podmienić torcha; tu ich runtime'owe zależności.
runpod>=1.7.9,<2
# qwen-tts 0.1.1 pinuje:
transformers==4.57.3
accelerate==1.12.0
onnxruntime
sox
# voxcpm 2.0.3 runtime:
einops
pydantic
regex
inflect
wetext
addict
simplejson
sortedcontainers
safetensors
torchcodec==0.7.*
# wspólne
huggingface_hub
librosa
soundfile
numpy
